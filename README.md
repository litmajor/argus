# Argus — SSA Process Monitor (scaffold)

Minimal scaffold for the ARGUS SSA-inspired process monitor.

Run:

```powershell
cargo run --manifest-path C:\Users\RYZEN\repos\litmajor\argus\Cargo.toml
```

This scaffold includes:

- `core::process::Process` model
- Minimal runtime module

Next steps: implement collectors, CPU sampling, and a TUI surface.
